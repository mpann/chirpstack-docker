{
	"connect_opts": {
		"host": "localhost",
		"port": 8080,
		"mail": "admin",
		"password": "admin"
	},
	"default_network_server_name": "network_server",
	"default_network_server_address": "cs-nw-server:8000",
	"default_service_profile_name": "service_profile",
	"default_device_profile_name": "device_profile",
	"notes": [
		"This is the default localhost host/port configuration from",
		"chirpstack documentation",
		"The password has not been commited for security reason",
		"but need to be set to change server configuration. Ask",
		"your IT service to get your credentials",
		"IMPORTANT: if you use the chirpstack-docker, the default",
		"network server address should be the name of the container",
		"defined in the docker-compose file -> cs-nw-server.",
		"If you are using a localhost installation of chirpstack,",
		"the network server address is either localhost or 127.0.0.1.",
		"If you do not configure this well, the add_customer command",
		"will fail when trying to create the network server."
	]
}